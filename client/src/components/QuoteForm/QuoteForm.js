import "./QuoteForm.css";

const QuoteForm = () => {
	return (
		<form className="quote-form">
			<h2 className="quote-form__title">Quote Form</h2>
			<button className="quote-form__submit" type="submit">Submit</button>
		</form>
	);
};

export default QuoteForm;
